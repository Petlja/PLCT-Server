verbose: false
course_project_dirs: []

knowledge_sources:
  - key: courses
    type: plct-ai-ctx
    url: ai-context
  - key: handbook
    type: aikt-bundle
    url: "aikt-context"

knowledge_cache_dir: ".cache/knowledge"

# Stream what the AI pipeline logs to the chat client, where a toggle shows it next to
# the answer. Add `verbose: true` for the DEBUG detail as well.
debug_mode: false
